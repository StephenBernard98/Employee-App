import { Inter, Mooli, Nunito, Roboto } from "next/font/google";

export const mooli = Mooli({ subsets: ["latin"], weight: "400" });

export const inter = Inter({ subsets: ["latin"] });

export const roboto = Roboto({
  weight: "400",
  subsets: ["latin"],
});

export const nunito = Nunito({
  weight: "400",
  subsets: ["latin"],
});
